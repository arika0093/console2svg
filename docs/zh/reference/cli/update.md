---
title: update
description: 检查并执行 console2svg 更新的命令。
---

```bash
console2svg update [--check] [--force] [--yes]
```

`--check` 只检查，`--force` 即使在包管理器管理下也更新，`--yes` 省略确认提示。

## 选项

### `--check`

只检查可用更新，不安装。

### `-f, --force`

即使安装由包管理器管理也更新。

### `-y, --yes`

省略确认提示。
