---
title: 内置窗口边框
description: console2svg 中包含的窗口边框列表。
since: v0.10
---

这些内置主题不会更改终端配色，只会添加窗口边框。
请将 ID 传给 `-d` 或 `--window` 来指定。

## 窗口边框列表

### `macos`

{/* c2s::  -d macos -w 40 -h 4 -c -- dotnet --version */}
![dotnet --version](/assets/generated/e8f388497c56.svg)

### `macos-pc`

{/* c2s::  -d macos-pc -w 40 -h 4 -c -- dotnet --version */}
![dotnet --version](/assets/generated/85fc7ada3068.svg)

### `windows`

{/* c2s::  -d windows -w 40 -h 4 -c -- dotnet --version */}
![dotnet --version](/assets/generated/4e1fa268bddd.svg)

### `windows-pc`

{/* c2s::  -d windows-pc -w 40 -h 4 -c -- dotnet --version */}
![dotnet --version](/assets/generated/50ffc9733f24.svg)

### `none`

{/* c2s::  -d none -w 40 -h 4 -c -- dotnet --version */}
![dotnet --version](/assets/generated/171066a411e8.svg)

### `transparent`

{/* c2s::  -d transparent -w 40 -h 4 -c -- dotnet --version */}
![dotnet --version](/assets/generated/7a43acbe6332.svg)
