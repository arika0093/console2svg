---
title: 組み込みウインドウ枠
description: console2svg に含まれるウインドウ枠の一覧。
---

ターミナルの配色を変えずに、ウインドウ枠だけを付ける組み込みテーマです。
`-d` または `--window` にIDを渡して指定します。

## ウインドウ枠一覧

### `macos`

{/* c2s:: -d macos -w 40 -h 4 -c -- dotnet --version */}
![macOS window frame](../../../assets/window/macos.svg)

### `macos-pc`

{/* c2s:: -d macos-pc -w 40 -h 4 -c -- dotnet --version */}
![macOS PC window frame](../../../assets/window/macos-pc.svg)

### `windows`

{/* c2s:: -d windows -w 40 -h 4 -c -- dotnet --version */}
![Windows window frame](../../../assets/window/windows.svg)

### `windows-pc`

{/* c2s:: -d windows-pc -w 40 -h 4 -c -- dotnet --version */}
![Windows PC window frame](../../../assets/window/windows-pc.svg)

### `none`

{/* c2s:: -d none -w 40 -h 4 -c -- dotnet --version */}
![No window frame](../../../assets/window/none.svg)

### `transparent`

{/* c2s:: -d transparent -w 40 -h 4 -c -- dotnet --version */}
![Transparent window frame](../../../assets/window/transparent.svg)
