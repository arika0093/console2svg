---
title: completions
description: シェル補完スクリプトを生成するコマンド。
---

```bash title="Terminal"
console2svg completions generate <shell>
```

`bash`、`zsh`などのシェル名を指定して補完スクリプトを生成します。

## サブコマンドと引数

### `generate <shell>`

指定したシェル向けの補完スクリプトを生成します。対応するシェル名は`bash`、`zsh`、`fish`、`pwsh`などです。
