---
title: completions
description: 生成 Shell 补全脚本的命令。
---

```bash
console2svg completions generate <shell>
```

指定 `bash`、`zsh` 等 Shell 名称来生成补全脚本。

## 子命令和参数

### `generate <shell>`

为指定 Shell 生成补全脚本。支持的 Shell 名称包括 `bash`、`zsh`、`fish`、`pwsh` 等。
